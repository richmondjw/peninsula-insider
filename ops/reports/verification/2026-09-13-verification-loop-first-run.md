# PI-006 verification loop: report-only run

Generated by `next/scripts/run-verification-loop.mjs` as at 2026-09-13. Mode: **report-only**. Nothing in this run wrote to a content record, and no component in the loop can: the only writer in the directory refuses any path outside a reports tree, and the run hashes every content and data file before and after itself to prove it.

## Whether this run can be trusted

These checks are the point of the report-only stage. They are the properties a write stage would have to keep, measured against the real corpus, so the write-scope decision has evidence rather than assurances.

| Check | Result | What it means |
|---|---|---|
| no-content-write | pass | the report-only loop must not alter a single byte of any content or data record |
| no-check-date-refreshed | pass | a successful fetch proves a page responded, never that a person read it and agreed, so no composed change may move a verification date |
| no-evidence-row-recorded | pass | a confirmation produces a PROPOSED evidence row for a person to accept; recording one automatically is the same check-date failure wearing a different field name |
| every-change-cites-evidence | pass | every proposed change must cite the fetched artifact it came from, by digest |
| every-escalation-states-a-question | pass | an escalation without a specific question and a default is a notification, and notifications are what a review queue dies of |
| outbound-limited-to-cited-sources | pass | the only outbound traffic permitted is a GET of a URL a record already cites |
| no-verdict-without-a-readable-source | pass | unreachable and blocked are source-health facts, never verdicts about a claim |
| no-figures-in-the-report | pass | this publication carries no prices, and a rate finding must escalate without one riding along |
| no-em-dash-in-the-report | pass | BRAND-PI hard rule |

## What the loop refused to do

A refusal is the loop declining to compose a change it cannot support. This count matters more than the findings: a loop that never refuses anything is a loop with no guard, and the guard is the component a write stage has to trust.

None this run, and on a production run that is the expected reading rather than a clean bill of health. The researcher in this stage is a deterministic detector set, and it proposes only what a page can establish, so it rarely offers the guard anything to refuse. The guard is exercised instead by the seeded fixtures in `scripts/verification-loop/fixtures/seeded-failures`, where a simulated model-backed researcher proposes a bumped check date, a licence read off a credit line and a first-person visit, and `scripts/verification-loop.test.mjs` asserts that all three are refused. That is where a write stage should look for evidence, because that is the traffic a write stage would actually carry.
## The registry

362 claims, 429 evidence rows. State is derived, never stored, so these numbers move with the calendar and not with a migration.

| Claim state | Claims |
|---|---|
| unsupported | 236 |
| supported | 126 |

| Reader consequence | Claims |
|---|---|
| high | 222 |
| medium | 86 |
| low | 54 |

## The backlog, as a shape

298 evidence rows are expired, across 236 claims. That is the corpus arriving with a history, not a list of defects introduced this week, and working it as a queue would be a mistake. What matters is the shape: which classes, how overdue, and whether anybody can still reach the page the claim sits on.

| Claim class | Expired rows |
|---|---|
| event-status | 141 |
| conditions | 69 |
| booking | 38 |
| access-restriction | 27 |
| event-schedule | 18 |
| trading-status | 4 |
| offering | 1 |

| How overdue | Expired rows |
|---|---|
| over 3 periods overdue | 216 |
| 1 to 3 periods overdue | 55 |
| under 1 period overdue | 27 |

| Reader consequence | Expired rows |
|---|---|
| high | 237 |
| medium | 60 |
| low | 1 |


234 claims sit behind a record no reader can reach: an archived event, a lapsed note, a date that has passed. They stay counted here and stay out of the worklist, because re-fetching an organiser page for a festival that already ran is work with nobody on the other end of it. 50 more cite no fetchable URL at all and can never be checked from their own record.

## Blind spots

Claim classes the precedence table defines and the corpus has no evidence for. The loop reports nothing about these, and that silence is an absence of looking rather than an all-clear. Naming them is the only honest thing to do with a gap.

| Claim class | What it covers | Claims | Evidence |
|---|---|---|---|
| opening-hours | When it is open | 0 | 0 |
| rate-change | A rate moved | 0 | 0 |
| accessibility | Access details | 0 | 0 |


## This run

25 claims were selected from 127 eligible, ranked by reader consequence and how far past expiry they are, capped at 25. Selection is deterministic: two runs over an unchanged corpus pick the same batch, which is what makes comparing the loop against a human possible.

| Adjudication | Claims |
|---|---|
| confirmed | 16 |
| blocked | 6 |
| inconclusive | 2 |
| unreachable | 1 |

| Source reachability | Claims |
|---|---|
| ok | 18 |
| blocked | 6 |
| dead | 1 |

_Nothing to report._


7 of 25 sources (28%) could not be read. That is a fact about the citations, not about the claims, and it is kept out of the findings for exactly that reason: a checker that files "I could not look" as "this is wrong" produces a report nobody reads twice.

## The queue

Every row states a specific decision and what happens if nobody takes it. A finding that cannot be written that way is a finding the loop has not thought through, and the deterministic gate fails the run over it.

| Kind | Subject | Consequence | Decision | If no answer |
|---|---|---|---|---|
| blocked | data-facts/beaches/balnarring-beach | high | the cited source answered and refused this client (HTTP 403). The cited source refuses automated clients, so this claim can never be checked this way. Accept manual re-checks, or cite a readable source? | no change is made and the claim stays unsupported |
| unreachable | events/emu-plains-market | high | the cited source could not be read (HTTP 404). The cited source cannot be reached. Replace the citation, or retire the claim? | no change is made and the claim stays unsupported |
| inconclusive | quick-notes/2026-09-13-weather-sunday | high | the citation resolves to application/json, which is a data endpoint rather than a page. The citation is a data endpoint, not a page, so no prose check can ever reach a verdict on it. Cite a readable page, or move this claim class to a checker that reads the feed? | no change is made and the claim stays unsupported |
| blocked | species/australian-salmon | high | the cited source answered and refused this client (HTTP 403). The cited source refuses automated clients, so this claim can never be checked this way. Accept manual re-checks, or cite a readable source? | no change is made and the claim stays unsupported |
| blocked | species/bream | high | the cited source answered and refused this client (HTTP 403). The cited source refuses automated clients, so this claim can never be checked this way. Accept manual re-checks, or cite a readable source? | no change is made and the claim stays unsupported |
| blocked | species/flathead | high | the cited source answered and refused this client (HTTP 403). The cited source refuses automated clients, so this claim can never be checked this way. Accept manual re-checks, or cite a readable source? | no change is made and the claim stays unsupported |
| inconclusive | events/foxeys-hangout-vegetable-feast-morning-sun-vineyard | high | the cited page does not name the subject, so it cannot confirm or contradict it (matched 2 of 6 identifying words). The cited source no longer identifies this subject. Repoint the citation, or retire the claim? | no change is made and the claim stays unsupported |
| blocked | species/garfish | high | the cited source answered and refused this client (HTTP 403). The cited source refuses automated clients, so this claim can never be checked this way. Accept manual re-checks, or cite a readable source? | no change is made and the claim stays unsupported |
| blocked | data-facts/beaches/flinders-beach-western-port-north-end | high | the cited source answered and refused this client (HTTP 429). The cited source refuses automated clients, so this claim can never be checked this way. Accept manual re-checks, or cite a readable source? | no change is made and the claim stays unsupported |


## Proposed changes

None. The loop composed no field-level change this run.

## What a write stage would still need

- A run of this loop against human decisions on the same batch, so the critic has a measured false-positive and false-negative rate rather than an assumed one.
- A decision on write scope (D5). Nothing here presumes one, and the guard list in `compose.mjs` is the narrowest defensible starting point, not a proposal.
- Evidence coverage for the three blind-spot classes. A write stage that cannot see opening hours, accessibility or rate changes would be writing with a third of the reader-facing surface out of view.
- An owner for the dead and blocked citations. Roughly a third of this corpus cannot be checked from its own recorded source, and no amount of loop improves that number.
