# Map coordinate land-mask audit — 2026-08-21

Reverse-geocoded 218 venue/experience/place coordinates against OpenStreetMap Nominatim. This is the fine-grained follow-up to `lint:coordinates` (bounding box only) - see that script's header comment for why both exist.

**0 flagged as water or no reverse match. 0 lookup errors (network/rate-limit, not necessarily bad data — re-run to confirm).**

