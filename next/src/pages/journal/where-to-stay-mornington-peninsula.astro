---
import Redirect from '../../components/Redirect.astro';
export const prerender = true;
---
{/* SEOMIG 2026-07-11: retargeted from /explore/where-to-base-yourself/ (itself
    a §3.2 consolidation loser) directly to the FINAL canonical, so this stub
    never becomes a two-hop chain when the stay cluster consolidates. */}
<Redirect to="/stay/best-accommodation/" />
