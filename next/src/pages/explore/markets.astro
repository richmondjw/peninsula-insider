---
/** Original market comparison recommended the now-closed Red Hill Community Market.
 * Consolidate on the current market guide until every comparison is reverified. */
import Redirect from "../../components/Redirect.astro";
export const prerender = true;
---
<Redirect to="/eat/markets/" consolidate />
