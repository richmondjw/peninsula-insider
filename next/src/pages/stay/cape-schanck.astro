---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import VenueCard from '../../components/VenueCard.astro';
import NewsletterBlock from '../../components/NewsletterBlock.astro';
import { routeSlug } from '../../lib/editorial';
import { buildBreadcrumbSchema, absUrl } from '../../lib/schema';

const allVenues = await getCollection('venues');
// One Spa at RACV Cape Schanck Resort is a day spa, not accommodation, and
// has no overnight sibling in the catalogue - The Cape Retreat is the
// genuine overnight stay in this locality.
const slugs = ['cape-retreat'];
const venues = slugs.map((slug) => allVenues.find((venue) => routeSlug(venue) === slug)).filter((venue): venue is NonNullable<typeof venue> => Boolean(venue));
const breadcrumbItems = [{ label: 'Home', href: '/' }, { label: 'Stay', href: '/stay/' }, { label: 'Cape Schanck' }];
const breadcrumbSchema = buildBreadcrumbSchema([{ name: 'Home', url: absUrl('/') }, { name: 'Stay', url: absUrl('/stay/') }, { name: 'Cape Schanck', url: absUrl('/stay/cape-schanck/') }]);
export const prerender = true;
---
<BaseLayout title="Where to Stay in Cape Schanck · Peninsula Insider" description="Where to stay in Cape Schanck on the Mornington Peninsula, for resort-led, golf-adjacent, and slower coastal retreat weekends." section="stay" canonical="https://peninsulainsider.com.au/stay/cape-schanck/" modifiedTime="2026-04-24">
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />
  <Breadcrumbs items={breadcrumbItems} />
  <article class="article"><div class="container"><h1 class="article__title">Where to Stay in Cape Schanck</h1><div class="prose"><p>Cape Schanck is not a town-base choice in the same way Sorrento or Mornington are. It is a retreat-base choice. You stay here for wind, cliffs, golf, spa time, and distance from busier Peninsula rhythms. That makes it useful for readers who want the trip to feel sealed off rather than socially full.</p><p>The category is therefore narrow but clear. If Cape Schanck is the right answer, it is because the weekend is about reset, not browsing.</p><p style="font-size:0.8rem;color:var(--soft)">Last fact-verified: 24 April 2026</p></div></div></article><section class="venues"><div class="container"><div class="venues__header"><div><p class="label label--accent">Cape Schanck stays</p><h2 class="venues__title">Retreat-led Cape Schanck base</h2><p class="venues__sub">For golf, spa, weather, and a more sealed-off southern Peninsula weekend.</p></div><a href="/explore/places/cape-schanck/" class="venues__link">Cape Schanck guide →</a></div><div class="venues__grid">{venues.map((venue) => <VenueCard venue={venue} hrefPrefix="/stay" />)}</div></div></section><NewsletterBlock /></BaseLayout>
