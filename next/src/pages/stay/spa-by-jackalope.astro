---
import Redirect from '../../components/Redirect.astro';

/**
 * Spa by Jackalope is a day spa, not accommodation (the overnight option is
 * Jackalope Hotel itself, which keeps its own /stay/ page). Keep the old
 * inbound URL alive while sending readers to its canonical wellness
 * editorial entry.
 */
---
<Redirect to="/explore/spas-and-wellness/#spa-by-jackalope" consolidate />
