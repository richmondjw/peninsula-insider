# PI weekly social pack — week of 2026-08-17

Three platform-ready posts built around the live service layer. `index.html` is the editable review board and source; `exports/` contains the 4:5 PNG masters captured from it.

Buffer drafts use current public hero imagery so they remain valid before this asset pack is deployed. The rendered PNGs are the review-ready publishing masters; a later site/media upload can swap the draft image URLs for the completed branded assets without changing the copy or destination links.
